---
description: Prune Docker's build cache, unused images, stopped containers, and networks to reclaim disk space — safe by default, deep clean only with confirmation.
---

The canonical harness-shared skill body was pruned from `.agents/skills/`; the command itself lives at user scope in `~/.claude/commands/docker-clean.md`, which this stub mirrors verbatim. This copy exists only for `/docker-clean` discoverability in Claude Code — see the open TODO.md item about retiring the tracked duplicates.
