# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.10.0
# Date: 2026-07-19T07:02:27.699Z

# Deliberately duplicated from `_bmad/custom/config.user.toml`, which is the
# durable home for this override. Both are required: 33 BMM skills read THIS
# file directly, while only 6 call `_bmad/scripts/resolve_config.py` and see
# the four-layer TOML merge. Setting the TOML alone leaves the value inert for
# almost every skill.
#
# This file is installer-generated, so `npx bmad-method install` will reset it
# to "intermediate" — re-apply this line after any BMAD upgrade. The TOML copy
# survives the installer and is the source of truth for intent.
user_skill_level: expert
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"

# Core Configuration Values
user_name: Kevinkhanhle
project_name: sensebridge
communication_language: English
document_output_language: English
output_folder: _bmad-output
