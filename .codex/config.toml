# SenseBridge-specific Codex settings. Global defaults live in
# `~/.codex/config.toml`; keep this file limited to repository behavior.
# Never add API keys, tokens, or machine-local credentials here — and no
# absolute home-directory paths: this file is public and shared across clones.
# If a binary is not on PATH, fix that in the machine-local ~/.codex/config.toml.

[features]
hooks = true

# Serena provides symbolic code navigation/editing, project-scoped. NO_COLOR
# keeps ANSI codes out of the stdio JSON-RPC stream.
["start-mcp-server", "--project-from-cwd", "--context=codex"]
startup_timeout_sec = 60

