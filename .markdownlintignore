LICENSE

# Local, untracked private notes — not shipped, not worth formatting. Its
# public counterpart NOTES.md IS linted; do not add it here.
NOTES.local.md

# Audit reports are append-only and never rewritten in place — see
# audits/GOVERNANCE.md. Formatting issues in a filed report are left as-is.
audits/*/[0-9]*.md

# Vendored `impeccable` skill content installed via `npx impeccable install`
# into each agent tool's skills directory — not hand-authored, re-synced from
# the upstream package, so left unformatted.
**/skills/impeccable/**

# Vendored offline slice of `AgriciDaniel/claude-seo` (see CREDITS.md), kept
# close to upstream formatting so future update diffs stay readable; mirrors
# are generated from the `.claude` canonical by tools/sync-skills.mjs.
**/skills/seo-schema/**
**/skills/seo-technical/**

# Template mirrors a user-global file, not repo prose — excluded so lint
# rules never force edits that would drift it from its upstream shape.
CLAUDE.template.md

# Generated by graphify (`graphify update .`) — not hand-authored, already
# gitignored; excluded here too so the CLI matches editor behavior.
graphify-out/

# Installed dependency trees, never hand-authored — excluded so the CLI
# matches editor behavior (editors already ignore these via .gitignore).
**/node_modules/**

# Vendored BMAD Method skill content installed by the bmad-method setup —
# not hand-authored, re-synced from upstream, so left unformatted for the
# same reason as the impeccable mirrors above.
**/skills/bmad-*/**

# Vendored `stripe/ai` skills, pinned by upstream content hash in
# skills-lock.json. Reformatting them would break that hash check, so they
# are excluded rather than fixed.
**/skills/stripe-best-practices/**
**/skills/stripe-projects/**
**/skills/upgrade-stripe/**

# Vendored `react-doctor` skill mirrors, re-synced from upstream by
# tools/sync-skills.mjs — same rationale as the other vendored skill trees.
**/skills/react-doctor/**

# Gitignored working files: hour-bucketed session logs and scratch output.
# Neither ships, and both are rewritten constantly — the same reasoning that
# exempts NOTES.local.md above.
sessions/
tmp/
