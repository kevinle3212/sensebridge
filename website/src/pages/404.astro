---
/**
 * Not Found. Kept as a dedicated page file (rather than generated by
 * [status].astro) because Astro emits it flat as `dist/404.html`, which is the
 * file static hosts serve for an unmatched path.
 */
import ErrorPage from "../components/ErrorPage.astro";
---

<ErrorPage code={404} locale="en" />
