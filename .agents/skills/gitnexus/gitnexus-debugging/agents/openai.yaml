interface:
  display_name: "Gitnexus Debugging"
  short_description: "the user is debugging a bug, tracing an error, or asking"
  default_prompt: "Use $gitnexus-debugging when the user is debugging a bug, tracing an error, or asking why something fails."
